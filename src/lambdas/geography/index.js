// const { createLambda } = require("../../utilities/create_lambda");
const { server } = require("./geography");

// const geography_handler = createLambda(handler);

return server;

// module.exports = {
//   geography_handler,
// };
